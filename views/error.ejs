<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Hashmicro Coding Test</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        .error-container {
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .error-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            max-width: 500px;
            width: 100%;
            margin: 20px;
        }
        .error-icon {
            font-size: 4rem;
            color: #dc3545;
            margin-bottom: 20px;
        }
        .error-code {
            font-size: 6rem;
            font-weight: 700;
            color: #667eea;
            line-height: 1;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card">
            <div class="text-center p-5">
                <% if (typeof error !== 'undefined' && error.status === 404) { %>
                    <div class="error-code">404</div>
                    <i class="fas fa-search error-icon"></i>
                    <h2 class="mb-3">Page Not Found</h2>
                    <p class="text-muted mb-4">The page you are looking for doesn't exist or has been moved.</p>
                <% } else { %>
                    <div class="error-code">500</div>
                    <i class="fas fa-exclamation-triangle error-icon"></i>
                    <h2 class="mb-3">Something Went Wrong</h2>
                    <p class="text-muted mb-4"><%= message || 'An unexpected error occurred. Please try again later.' %></p>
                <% } %>
                
                <div class="d-flex gap-2 justify-content-center">
                    <a href="/dashboard" class="btn btn-primary">
                        <i class="fas fa-home me-2"></i>Go to Dashboard
                    </a>
                    <button onclick="history.back()" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-2"></i>Go Back
                    </button>
                </div>
                
                <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
                    <div class="mt-4">
                        <details>
                            <summary class="btn btn-outline-danger btn-sm">
                                <i class="fas fa-bug me-2"></i>Show Error Details (Development)
                            </summary>
                            <div class="mt-3 p-3 bg-light rounded text-start">
                                <pre class="text-danger small"><%= error.stack %></pre>
                            </div>
                        </details>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-redirect after 10 seconds for 404 errors
    <% if (error && error.status === 404) { %>
                setTimeout(() => {
                    window.location.href = '/dashboard';
                }, 10000);
            <% } %>
    </script>
</body>
</html>